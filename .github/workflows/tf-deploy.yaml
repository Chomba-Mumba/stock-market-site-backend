name: 'Deploy AWS Resources'

on:
  workflow_run:
    workflows: ["Python Lambda Tests"]  # The name of the test workflow
    types:
      - completedRun tests on pull requests to the main branch
env
  TF_CLOUD_ORGANIZATION: "personal_chomba"
  TF_API_TOKEN: ${{ secrets.TF_API_TOKEN }}
  TF_WORKSPACE: ${{ secrets.TF_WORKSPACE }}
  CONFIG_DIRECTORY: "terraform"

jobs:
  terraform:
    if github.repository != 'personal_chomba/stock-market-site':
    name: 'Terraform Apply'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Upload Configuration
        uses: hashicorp/tfc-workflows-github/actions/upload-configuration@v1.0.0
        id: apply-upload
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          directory: ${{ env.CONFIG_DIRECTORY }}

      - name: Create Apply Run
        uses: hashicorp/tfc-workflows-github/actions/create-run@v1.0.0
        id: apply-run
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          configuration-version: ${{ steps.apply-upload.outputs.configuration_version_id }}
          plan-only: true

      - name: Apply
        uses: hashicorp/tfc-workflows-github/actions/apply-run@v1.0.0
        if: fromJSON(steps.apply-run.outputs.payload).data.attributes.actions.IsConfirmable
        id: apply
        with:
          run: ${{ steps.apply-run.outputs.run_id }}
          comment: "Apply Run from GitHub Actions CI ${{ github.sha }}"